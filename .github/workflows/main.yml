name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.67eb4736-544a-42f6-936e-a174975b4923}}
          heroku_app_name: "idlgateway" #Must be unique in Heroku
          heroku_email: "saman.barakat@gmail.com"
